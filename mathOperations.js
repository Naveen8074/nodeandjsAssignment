var a = 10;
var b = 5;
var sum = a + b; 
console.log(`The sum of ${a} and ${b} is: ${sum}`);


let x = 20;
let y = 4;
let product = x * y; 
console.log(`The product of ${x} and ${y} is: ${product}`);

const radius = 7;
const pi = 3.14159;
const area = pi * radius * radius; 
console.log(`The area of a circle with radius ${radius} is: ${area}`);

let base = 10;
const height = 15;
var areaOfTriangle = (base * height) / 2; 
console.log(`The area of a triangle with base ${base} and height ${height} is: ${areaOfTriangle}`);
